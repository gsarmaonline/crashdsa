name: Deploy to Fly.io

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows manual deployment from Actions tab

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run tests
        run: bun test

      - name: Type check
        run: bun run --bun tsc --noEmit || echo "No tsconfig or type errors found"

  deploy:
    name: Deploy to Fly.io
    needs: test # Only run if tests pass
    runs-on: ubuntu-latest

    # Only deploy on main branch (extra safety)
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run database migrations
        run: bunx prisma migrate deploy
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Seed database
        run: bun run db:seed
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Setup Flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy to Fly.io
        run: flyctl deploy --remote-only
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Deployment summary
        run: |
          echo "✅ Deployed to Fly.io successfully!"
          echo "🚀 App URL: https://crashdsa.fly.dev"
          echo "📊 Check status: flyctl status -a crashdsa"
